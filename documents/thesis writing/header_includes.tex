% Injected into the Pandoc-generated PDF (built via xelatex, for native
% Unicode support -- the source markdown uses real minus/degree/sigma
% characters directly rather than LaTeX math escapes). This is the ONLY
% LaTeX involved -- Pandoc handles every table, list and heading
% automatically, so there is no hand-tuned column-width code here to go wrong.
\usepackage{booktabs}   % clean horizontal rules in tables
\usepackage{longtable}  % lets wide/tall tables break across pages
\usepackage{array}
\pagestyle{plain}       % page number, bottom centre
\setlength{\emergencystretch}{3em}  % avoid overfull lines in justified text
\raggedbottom
